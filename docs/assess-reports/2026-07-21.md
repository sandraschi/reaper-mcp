# Assessment Report — reaper-mcp

**Date**: 2026-07-21
**Repo Type**: Standard MCP Server (FastMCP 3.4.4) with webapp + Tauri native
**SOTA Score**: **87/100** (SOTA — minor gaps)

---

## Score Breakdown

Base: 100. Deductions:

| Category | Deduction | Detail |
|----------|-----------|--------|
| Required files | -2 | Missing CLAUDE.md, .claude-plugin/ |
| Tool surface | -2 | Portmanteau tools use Args: instead of Annotated+Field |
| Testing | 0 | 6 tests exist (unit + integration) |
| Webapp SOTA | -5 | Missing Zustand, useZoom, keyboard shortcuts, API Docs page |
| CORS | 0 | Fleet-standard with Tailscale+LAN+Tauri |
| Security | 0 | .env.example exists, no CORS wildcard |
| Tauri | 0 | Proper backend.rs with stream watching, multi-layer free_port |
| FastMCP | 0 | Has resources, prompts (6), skills, annotations |
| Bad patterns | 0 | No print(), no bare except, no console.log |
| Error handling | 0 | Proper logging, no bare except:pass |
| CI/CD | 0 | Has .github/workflows/ci.yml |
| Gate failures | 0 | lint=clean, format=clean, tsc=clean, tests=? |
| Dockerization | 0 (INFO) | No Docker files — not a container stack |

## Gaps to Fix

### MEDIUM
1. **CLAUDE.md** — missing per-repo agent instructions
2. **.claude-plugin/** — missing for Claude Code session injection
3. **.opencode/skills/** — missing for OpenCode session injection
4. **Zustand** — missing from package.json (using @tanstack/react-query)
5. **useZoom hook** — Ctrl+Scroll zoom not implemented
6. **Keyboard shortcuts** — Ctrl+L/H/K/0 not implemented
7. **API Docs page** — no /api-docs route or page
8. **color-scheme: dark** — verify index.css
9. **Tool docstrings** — portmanteau tools use Args: instead of Annotated[..., Field(description=...)]
10. **Exponential backoff** on health check - check dashboard
11. **justfile missing** serve, test, fmt, types, gates-green, mcpb-pack, e2e

### LOW
1. **.bak dross** — 6 stale .bak files
2. **.assess-fix-timestamp** — missing
3. **docs/assess-reports/** — missing (now created)
